import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color error = Colors.red;

  // Light mode ------------------------------------------------

  static const Color light = Colors.orange;
  static const Color lightop = Colors.black;
  static const Color lightBg = Colors.white;

  // Dark mode ------------------------------------------------

  static const Color dark = Colors.black;
  static const Color dark2 = Color(0xFF2C2C2C);
  static const Color darkop = Colors.white;
  static const Color darkBg = Color.fromARGB(255, 100, 100, 100);
}
